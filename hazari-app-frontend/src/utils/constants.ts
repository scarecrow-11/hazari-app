import type { ThemeConfig } from 'antd'

export const MAX_NUMBER_OF_CARDS = 13

export const DECK_IMAGE_BASE_URL = '/images/deck'

export const PATHS = {
    PLAY: '/play',
    HAND_MAKER: '/hand-maker'
}

export const THEME_CONFIG: ThemeConfig = {
    token: {
        colorPrimary: '#2aaa8a'
    }
}